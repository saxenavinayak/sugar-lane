apiVersion: v1
kind: Service
metadata:
  name: switch-service
spec:
  type: NodePort
  selector:
    app: switches
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 32401
      nodePort: 32401
