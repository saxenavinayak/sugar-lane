apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: switches
  name: switch-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: switches
  template:
    metadata:
      labels:
        app: switches
    spec:
      containers:
      - env:
        - name: KASA_USERNAME
          valueFrom:
            secretKeyRef:
              key: kasa-username
              name: kasa-creds
        - name: KASA_PASSWORD
          valueFrom:
            secretKeyRef:
              key: kasa-password
              name: kasa-creds
        image: saxenavinayak/sugar-lane:ea13ab0b87659f043baaf899719377144444dcdb
        imagePullPolicy: Always
        name: switch
        ports:
        - containerPort: 8080
      imagePullSecrets:
      - name: regcred
